# 贡献指南

metweave 目前由作者独立维护，**暂不接受外部代码贡献**（PR 暂不开放）。使用中遇到问题欢迎开 issue 反馈（模板见 [`.github/`](.github/)）；安全问题请走 [SECURITY.md](SECURITY.md) 列出的私密渠道。开放贡献后，本文件会更新为完整的贡献指南。

## 文档书写标准（2026-09-25 定）

本仓库所有对用户可见的文字——README（根与各包）、CHANGELOG、docs/、代码注释、测试名、页面文案、release notes——的读者都是**外部用户**，一律用「项目对用户说话」的口吻：

- **只讲行为与承诺**：这个版本多了什么能力、行为怎么变了、用户怎么用；不写来历（谁下的指令、第几轮迭代、内部任务编号如 W39/批 3#12/方案B）；
- **CHANGELOG 按用户价值归组**：每条两三行、API 名与行为变更为主；没有实际变更的验证性陈述（如「存量行为零变化」）不进变更段；内部测试统计不设「测试」段（测试规模见 README 与 CI）；
- **CHANGELOG 分段基准＝相对上一个发布版本**：「新增」＝本版本带来的新能力；「变更」＝对上一发布版本既有行为的改动；「修复」＝对上一发布版本已交付行为的缺陷修正——新功能开发期发现并修正的问题属于新功能本身的完成度，不进「修复」（对外从未存在过坏版本）；
- **不用内部称谓**：owner/维护者指令、学习线、考核、判卷、施工图、二轮三轮、挂起/拍板等词不出现；治理语义需要时用「维护者（maintainer）」；
- **内部出处转译为对外出处**：内部清单的出处一律落到规范条文（WMO/AP-117 条款号）或「真实报文实测」；
- **注释同纪律**：代码注释讲约束不讲来历（改什么为什么约束，不写「某月某日按某指令改」）。

写入即门禁语义：新增对外文案若破坏上述口吻，视同缺陷修复。
