# examples

metweave 示例与 demo。每个示例自包含、可独立运行。

首个端到端示例（随 v0.1.0）：公开 METAR 报文 → 解析 → 地图渲染。

Examples and demos for metweave. Each example is self-contained and runnable.

First end-to-end example (with v0.1.0): public METAR report → parse → map rendering.
